<%= form_for @member_group do |f| %>
	<%= f.label :fullname %>
	<%= f.text_field :fullname %>
	<%= f.label :Group %>
	<%= f.select :group_id, options_for_select(Group.all.map{|c| [c.group_name, c.id]}) %>
	<%= f.submit "Create" %>

<% end %>